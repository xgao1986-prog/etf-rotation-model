# A股ETF轮动量化策略 v1.1

基于趋势跟踪+动量轮动的A股行业ETF量化交易策略，支持本地持久化运行。

**v1.1 代号**: "Defense + Trading Rules" — 防御资产填充 + 交易规则完善

---

## 版本历史

| 版本 | 日期 | 代号 | 核心变更 |
|------|------|------|----------|
| **v1.1** | 2026-06-13 | Defense + Trading Rules | 防御资产填充、宽基实验模块、交易规则完善、CLI/UI一致性 |
| v1.0 | 2025-06-12 | 本地化重构 | 基础轮动+防御、未来数据保护、仓位控制、交易费率、本地持久化 |

### v1.1 功能更新

#### 1. 防御资产填充（核心收益来源）
- 当行业ETF信号不足时，用**黄金ETF (518880.SH)** + **国债ETF (511010.SH)** 填充剩余仓位
- **回测验证**: 开启防御填充后，总收益从 50% → **104.76%**（+54.7%），夏普 0.51 → **0.78**（+0.27），最大回撤 -21.6% → **-14.41%**（-7.2%）
- 填充上限 30%（牛市），熊市 50%（可配置）
- 大盘择时默认**关闭**（回测验证开启反而降低收益）

#### 2. 宽基ETF补仓（实验模块，默认关闭）
- 新增 4 只宽基ETF：510300.SH（沪深300）、510500.SH（中证500）、159915.SZ（创业板）、588000.SH（科创50）
- 作为行业ETF不足时的第二层补充
- **默认关闭**（回测验证：开启后收益 -11.7%，回撤 +4%，夏普 -0.19）
- 保留为实验开关，v1.2 可能通过更严格的触发条件重新启用

#### 3. 三类资产分离评分
- **行业ETF**：完整评分体系（趋势30 + 确认20 + 动量25 + 成交量15 + 波动率10），含横截面动量排名
- **宽基ETF**：简化评分（无横截面动量，仅趋势+确认），总评分门槛 25
- **防御资产**：简化评分，总评分门槛 30

#### 4. 交易规则完善
- **交易执行价格**：用 T 日 **开盘价** 执行买卖（消除 look-ahead bias），T 日收盘价计算持仓净值
- **止损**：-8% 触发，每日检查，止损后 5 天冷静期
- **追踪止损**：tiered 模式（10% 仓位 / 6% 回撤；>10% 仓位 / 8% 回撤）
- **再平衡**：每周五（默认），交易日跳过则顺延
- **佣金**：0.03% 双向，最低 5 元

#### 5. CLI/UI 一致性修复
- `main.py signal` 和 `app.py` 的"重新计算评分"均复用 3-tier 评分流水线（与回测完全一致）
- `app.py` 第 1007 行嵌套缩进错误修复（`_get_ticker_name()` 被嵌套在 `if` 块中）
- `app.py` 信号展示按资产类型分组（行业 / 宽基 / 防御）

#### 6. 数据获取完善
- `data_fetcher.py` 自动下载全部 22 只标的（16 行业 + 4 宽基 + 2 防御）
- 双数据源：iFinD（主，高质）+ AKShare（备，免费）

#### 7. 可配置参数
- 初始资金可调（10万 ~ 1亿）
- `max_position_per_etf`（默认 15%）
- `max_holdings`（默认 5 只）
- `defense_fill_max_ratio_bull`（默认 30%）
- `market_timing`（默认 False）
- `fallback_equity_enabled`（默认 False）
- `stop_loss`（默认 -8%）
- `trailing_stop_mode`（默认 tiered）

### v1.0 → v1.1 关键修正

| 问题 | v1.0 状态 | v1.1 修正 |
|------|----------|----------|
| 结构性空仓（无防御时现金闲置） | 收益 50%，夏普 0.51，回撤 -21.6% | 防御填充，收益 **104.76%**，夏普 **0.78**，回撤 **-14.41%** |
| 交易执行价格（收盘价 → 开盘价） | 收盘执行存在 look-ahead bias | 开盘执行买卖，收盘计算净值 |
| 大盘择时默认开启 | 开启反而降低收益 | 默认关闭（验证更优） |
| 评分流水线不一致 | CLI signal 和 UI 复算与回测逻辑不同 | 统一 3-tier 流水线 |
| 宽基模块未定义 | 无 | 定义为实验模块，默认关闭 |
| app.py 缩进错误 | `_get_ticker_name()` 嵌套错误 | 修复嵌套，所有 py_compile 通过 |

---

## 项目结构

```
etf_rotation_model/
├── database/
│   └── etf_model.db              # SQLite数据库（~29,000条，22只标的）
├── src/
│   ├── config.py                  # 全局配置（22只标的 + 评分参数 + 默认开关）
│   ├── database.py                # 数据库操作封装（8张表）
│   ├── data_fetcher.py            # 数据获取（iFinD主 + AKShare备，自动22只）
│   ├── strategy.py                # 策略引擎（3-tier评分：行业/宽基/防御）
│   └── backtest.py                # 回测引擎（3-tier买入：行业→宽基→防御）
├── reports/                       # 回测报告（CSV + MD，.gitignore 不纳入）
├── signals/                       # 交易信号（CSV，.gitignore 不纳入）
├── scripts/                       # 实验脚本（纳入版本控制）
│   ├── parameter_sensitivity.py   # 参数敏感性实验（38组）
│   └── test_full_allocation.py    # 满仓能力实验（20%上限+不同填充）
├── main.py                        # CLI主入口（3-tier评分，信号/回测/状态）
├── app.py                         # Streamlit可视化界面（交互式调参）
├── requirements.txt               # Python依赖
├── HANDOFF.md                     # 工作交接上下文（每会话更新）
├── PROGRESS.md                    # 项目进展追踪
├── COLLABORATION.md             # Kimi×Codex协作规则
└── README.md                      # 本文件
```

---

## 快速开始

### 1. 安装依赖

```bash
cd etf_rotation_model
pip install -r requirements.txt
```

### 2. 下载历史数据

**方式A: iFinD数据（推荐，质量更高）**

在Kimi对话中：
```
用iFinD下载ETF历史数据，从2019-06-03开始
```

**方式B: AKShare数据（本地自动）**

```bash
python main.py update --full
```

> 建议：先用方式A获取iFinD数据，再用方式B补充缺失部分。

### 3. 日常数据更新

**iFinD增量更新（Kimi对话）:**
```
更新iFinD数据到最新
```

**AKShare增量更新（本地）:**
```bash
python main.py update
```

### 4. 回测

```bash
# 全区间回测
python main.py backtest --save

# 样本内回测 (2019-2023)
python main.py backtest --sample in --save

# 样本外验证 (2024-至今)
python main.py backtest --sample out --save
```

### 5. 生成交易信号

```bash
python main.py signal --save
```

### 6. 查看状态

```bash
python main.py status
```

### 7. 启动可视化界面

```bash
streamlit run app.py
```

---

## 评分体系

### 行业ETF（完整评分，满分100）

| 维度 | 权重 | 计算方式 |
|------|------|----------|
| 趋势强度 | 30% | 收盘价>20日均线(+15) + >50日均线(+10) + 均线斜率>0(+5) |
| 趋势确认 | 20% | 连续在20日均线之上的天数×4分，最多5天 |
| 动量 | 25% | 20日收益率的**横截面排名**（百分位×25） |
| 成交量 | 15% | 放量上涨(+15) / 放量(+10) / 普通(+5) |
| 波动率 | 10% | 适中波动率1-4%(+10) / 较高4-6%(+5) |

### 宽基ETF（简化评分，无横截面排名）

- 趋势强度 + 趋势确认，总评分门槛 ≥ 25
- 收盘价 > 20日均线 × 0.98，均线斜率 > -0.01
- **大盘强势条件**：收盘价 > MA50 且 MA50 斜率 > 0（`bull_market`）

### 防御资产（简化评分，无横截面排名）

- 趋势强度 + 趋势确认，总评分门槛 ≥ 30
- 收盘价 > 20日均线 × 0.98，均线斜率 > -0.001

---

## 入场/出场条件

### 入场（同时满足）

| 资产类型 | 总评分 | 收盘价条件 | 均线斜率 | 额外条件 |
|----------|--------|------------|----------|----------|
| 行业ETF | ≥ 40 | > 20日均线 | > 0 | 无 |
| 宽基ETF | ≥ 25 | > MA20 × 0.98 | > -0.01 | `bull_market=True`（大盘强势） |
| 防御资产 | ≥ 30 | > MA20 × 0.98 | > -0.001 | 无 |

### 出场（任一满足）

1. 收盘价跌破 20 日均线（信号失效）
2. 单只回撤超过 -8%（硬止损）
3. 追踪止损触发（tiered 模式：10%仓位/6%回撤；>10%仓位/8%回撤）

---

## 仓位控制

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `max_holdings` | 5 | 最多同时持有 5 只 |
| `max_position_per_etf` | 15% | 单只上限 15%（行业） |
| `defense_fill_max_ratio_bull` | 30% | 牛市防御填充上限（30% 即最多 2 只防御） |
| `stock_max_holdings` | 5 | 行业ETF最多 5 只 |
| `fallback_equity_max_holdings` | 3 | 宽基ETF最多 3 只（实验模块） |
| `defense_max_holdings` | 2 | 防御资产最多 2 只 |
| `total_max_holdings` | 5 | 总持仓上限（向后兼容） |

**满仓机制**：
- 15% 上限 × 5 只 = 75% 行业仓位
- 等权分配后，行业 + 防御通常 7-8 只，每只约 12-14%（低于 15% 上限，不触发截断）
- 30% 防御填充刚好填满剩余 25%，实现接近 100% 满仓

**实验发现**：
- 10% 上限：夏普最高（0.95），但收益较低（74.9%）
- **15% 上限（当前默认）**：收益最高（98.3%），夏普 0.85，回撤 -13.4% — 最佳平衡点
- 20% 上限 + 15% 填充：收益更高（107.1%），但回撤恶化到 -17.3%（夏普 0.73）

---

## ETF标的池

### 行业ETF（16只，核心轮动池）

| 代码 | 名称 |
|------|------|
| 512480.SH | 半导体ETF |
| 515230.SH | 软件ETF |
| 515880.SH | 通信ETF |
| 512010.SH | 医药ETF |
| 159928.SZ | 消费ETF |
| 516160.SH | 新能源ETF |
| 516110.SH | 汽车ETF |
| 512800.SH | 银行ETF |
| 512000.SH | 券商ETF |
| 512660.SH | 军工ETF |
| 512980.SH | 传媒ETF |
| 512400.SH | 有色金属ETF |
| 516120.SH | 化工ETF |
| 516960.SH | 基建ETF |
| 516650.SH | 煤炭ETF |
| 159915.SZ | 创业板ETF（在宽基中也可作为行业替代） |

### 宽基ETF（4只，实验模块，默认关闭）

| 代码 | 名称 |
|------|------|
| 510300.SH | 沪深300ETF |
| 510500.SH | 中证500ETF |
| 159915.SZ | 创业板ETF |
| 588000.SH | 科创50ETF |

### 防御资产（2只）

| 代码 | 名称 |
|------|------|
| 518880.SH | 黄金ETF |
| 511010.SH | 国债ETF |

---

## 回测结果（默认配置）

**配置**: `market_timing=False`, `fallback_equity_enabled=False`, `defense_enabled=True`, `rebalance_weekday=4`, `open_price` execution

| 指标 | 数值 |
|------|------|
| 总收益 | **104.76%** |
| 年化收益 | **10.53%** |
| 夏普比率 | **0.78** |
| 索提诺比率 | **0.96** |
| 最大回撤 | **-14.41%** |
| 交易次数 | 678 |
| 胜率 | 42.6% |
| 平均盈利 | 6.95% |
| 平均亏损 | -2.93% |
| 总佣金 | 43,392元 |
| 止损次数 | 14 |
| 平均持仓 | 3.3只 |
| 最大持仓 | 5只 |

**交易分布**:
- 行业ETF买入: 269
- 防御填充买入: 71
- 宽基补仓买入: 0（默认关闭）
- 防御配置买入: 0（market_timing=False）

---

## 数据源

### 双数据源架构

| 数据源 | 获取方式 | 质量 | 用途 |
|--------|---------|------|------|
| **iFinD** | Kimi对话中调用工具 | ⭐⭐⭐ 高 | 主数据源，优先使用 |
| **AKShare** | 本地Python自动获取 | ⭐⭐ 中 | 备用/补充，免费 |

### iFinD获取限制

- 每次查询最多 **10个ticker**
- 日期范围最多 **3年**
- 22只ETF需要分 **2-3批** 获取
- 2019-2026年需要分 **3段** 获取

---

## 已知问题与规划

| 问题 | 严重程度 | 计划版本 | 说明 |
|------|---------|----------|------|
| 结构性牛市跑输（2019-2021） | 高 | **v1.2** | 入场阈值太严格，行业信号不足。计划加入板块数据增强信号 |
| 宽基模块负贡献 | 中 | **v1.2** | 触发条件太宽（82.3%调仓日触发）。计划收紧条件、延长持有周期 |
| 主力资金数据缺失 | 高 | 待解决 | iFinD无直接接口，需寻找替代数据源 |
| 无基本面过滤 | 中 | 待接入 | ROE、营收增速等基本面数据 |

### v1.2 规划方向

- 板块指数数据接入（iFinD 申万行业指数）
- 板块动量增强（行业信号 + 板块信号双重确认）
- ETF 到板块映射
- 更严格的牛/熊定义（多均线、波动率、多时间框架）
- 宽基重新启用（更严格的触发条件 + 更长持有周期）

---

## 免责声明

本策略仅供学习和研究使用，不构成投资建议。量化交易有风险，入市需谨慎。

过往回测表现不代表未来收益。参数优化可能导致过度拟合，请谨慎对待历史回测结果。
