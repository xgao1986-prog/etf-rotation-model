# ETF 轮动模型当前主版本说明

> **文档口径**：主线口径澄清版
> **更新时间**：2026-06-21
> **当前分支**：`feature/v1.2.1-regime-adaptive`
> **数据截止**：2026-06-18
> **唯一当前基准**：**B0 系列的 18 只 ETF 基准版，冻结版本为 B0.3**

---

## 1. 当前真正的基准主线

本项目当前唯一用于策略评估、实验对照和版本演进的主线是：

> **B0 18只ETF基准版 → B0.3冻结基线**

其中：

- **B0** 表示当前18只ETF基准策略家族。
- **B0.3** 是该家族当前冻结的正式比较基线。
- 后续所有研究、诊断和功能实验，都必须以B0.3为A/B测试的对照组。
- 未经过独立验证并正式更新本文件的研究结果，不得取代B0.3成为新基线。

代码分支名称 `v1.2.1-regime-adaptive` 只是当前开发分支名称，不代表32只ETF增强池已经成为生产主线。

---

## 2. B0/B0.3基准包含什么

### 2.1 ETF池：固定18只

| 类型 | 数量 | 用途 |
|------|------|------|
| 行业ETF | 16只 | 评分、排序和轮动选择 |
| 防御ETF | 2只 | 行业信号不足时提供黄金和国债防御敞口 |
| 合计 | **18只** | 当前基准可交易池 |

16只行业ETF：

- 半导体、软件、通信、医药、消费、新能源、汽车、银行
- 券商、军工、传媒、有色金属、家电、养殖、油气、机器人

2只防御ETF：

- `518880.SH` 黄金ETF
- `511010.SH` 国债ETF

沪深300指数用于基准比较和市场观察，不属于18只可交易ETF。

### 2.2 当前核心配置

| 项目 | B0.3口径 |
|------|----------|
| 核心池 | 16只行业ETF |
| 防御池 | 黄金ETF、国债ETF |
| 调仓频率 | 每周四 |
| 最大持仓数 | 5只 |
| 单只仓位上限 | 20% |
| 动量排名因子 | 关闭 |
| 波动率因子 | 关闭 |
| 生效评分 | 趋势强度、趋势确认、成交量 |
| 止损 | 固定止损-8% |
| 调仓引擎 | v2.5纯函数调仓规划逻辑 |
| 市场状态 | 仅观察，不改变交易参数 |
| 宽基补仓 | 默认关闭 |

---

## 3. 32只ETF池不是当前基准

代码中存在由16只行业ETF和16只概念ETF组成的32只增强池框架，这是v1.2.1阶段搭建的研究与扩展能力。

必须明确：

- 32只ETF池是**研究/增强框架**，不是当前生产基线。
- 概念ETF数据完整性、历史可交易性和动态准入仍需进一步验证。
- `fixed_32` 回测使用了不同的标的池和信息集合，不能代表B0/B0.3主线表现。
- `fixed_32` 的收益、回撤、交易次数或归因结果，不得写成“当前模型表现”。
- 只有明确加载 `ETF_UNIVERSE + DEFENSE_UNIVERSE` 的18只ETF回测，才属于B0/B0.3口径。

如未来要采用32只增强池，必须将其作为独立实验组，与冻结B0.3进行完整A/B验证，而不能直接覆盖基线。

---

## 4. 当前交易执行口径

当前主线采用：

> **T日收盘后生成信号，T+1交易日开盘执行交易。**

实现口径：

- 趋势、均线、成交量等信号只使用截至T日收盘时已经可获得的数据。
- 回测在下一交易日使用开盘价执行买卖。
- 该口径避免使用尚未完成的当日收盘数据在同一收盘价成交。

注意：`src/config.py` 中 `EXECUTION_CONFIG['price_mode']='close'` 属于尚未接入当前回测执行路径的旧配置说明；实际基准成交口径以 `src/backtest.py` 使用当日 `open` 执行的实现为准。后续应单独清理这处文档性歧义，但不得在清理时改变B0.3交易结果。

---

## 5. 滑点口径

当前B0/B0.3基准暂不计滑点。

原因：

- 初始资金规模较小，对盘口冲击有限。
- 当前交易对象以流动性相对充足的ETF为主。
- 已计入交易佣金，现阶段滑点不是最优先的模型误差来源。

这不表示滑点永远可以忽略。进入更大资金规模、实盘准备或执行质量评估阶段后，应单独进行滑点压力测试，例如：

- 0 bps：当前基准
- 5 bps：常规压力
- 10 bps：保守压力
- 对低流动性ETF设置更高的个别压力参数

滑点测试属于稳健性验证，不能根据测试结果反向调优其他参数。

---

## 6. 当前主线表现以什么为准

当前主线评估只能使用B0/B0.3的长期回测和封存OOS结果。

### 6.1 B0.3长期基准

权威报告：`reports/baseline_B0.3_20260620_180745.md`

| 指标 | B0.3 |
|------|------|
| 最终NAV | 2,809,091 |
| 总收益 | 180.91% |
| 年化收益 | 16.99% |
| 夏普比率 | 0.8985 |
| 最大回撤 | -17.75% |
| 交易次数 | 801 |

上述结果对应冻结的18只ETF B0.3配置，不得与 `fixed_32` 或其他增强池结果混用。

### 6.2 封存OOS验证

权威报告：`reports/phase5_final_oos.md`

OOS区间为2025-01-01至2026-06-18。B0.3所采纳的 `no_momentum` 配置结果：

| 指标 | OOS结果 |
|------|---------|
| 年化收益 | 42.16% |
| 夏普比率 | 1.9086 |
| 最大回撤 | -11.36% |

该OOS区间已经封存，不得继续用于参数选择或反向调优。

---

## 7. Phase 5/6/7的正确定位

| 阶段 | 定位 | 是否生产基线 |
|------|------|--------------|
| Phase 5 | 因子、参数和OOS验证 | 否；验证结果可用于决定是否更新基线 |
| Phase 6 | 市场结构、止损、节假日、持仓与稳健性诊断 | 否 |
| Phase 7 | ETF池历史完整性、幸存者偏差等研究 | 否 |

这些阶段可能：

- 以B0.3为对照开展研究；
- 测试增强模块；
- 发现数据、回测或策略风险；
- 提出候选改进。

但在完成单变量A/B测试、样本内外验证、风险检查和正式基线更新之前，它们都不是生产基线。

---

## 8. 后续演进路线

后续固定按以下路线推进：

1. **B0 18只ETF基准版**
2. **稳健性验证**
3. **小步接入市场状态、持仓稳定或仓位优化**
4. **每次只修改一个变量并进行A/B测试**
5. **只有明确优于B0.3且风险未恶化的方案，才有资格成为候选**
6. **候选经正式验收后，才更新冻结基线和本文件**

建议优先级：

1. 先完成数据口径、交易记录、NAV和样本划分勾稽。
2. 再验证持仓稳定机制，观察是否减少无效换手。
3. 再以observer结果为基础，小步测试市场状态的单一影响。
4. 最后研究动态仓位优化，避免同时改变选股与仓位两套机制。

---

## 9. 项目统一锚点

后续讨论、报告和代码审查统一使用以下表述：

> **当前主线：B0 18只ETF基准版；当前冻结对照：B0.3。**
>
> **演进方法：稳健性验证 → 小步接入增强模块 → 每次只做一个变量的A/B测试。**

如其他文档、报告或聊天记录与本口径冲突，以本文件、B0.3基准脚本及新鲜回测验证结果为准。

---

*变更历史参见 `docs/CHANGES.md`。本文档只定义当前主线，不承载完整研究流水账。*
