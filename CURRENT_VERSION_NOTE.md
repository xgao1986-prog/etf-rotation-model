# ETF 轮动模型当前主版本说明

> **文档口径**：主线口径澄清版
> **更新时间**：2026-07-05
> **当前策略版本**：v1.2.3
> **当前分支**：`feature/paper-trading-ui-v0.3`
> **数据截止**：2026-06-29
> **唯一当前基准**：**B0.4 冻结基线**
> **虚拟实盘版本**：v0.3

---

## 1. 当前真正的基准主线

本项目当前唯一用于策略评估、实验对照和版本演进的主线是：

> **B0 18只ETF基准版 → B0.4 冻结基线**

其中：

- **B0** 表示当前18只ETF基准策略家族。
- **B0.4** 是该家族当前冻结的正式比较基线。
- **B0.3 已废止**：因尾部数据缺失（2026-06-08~12），无法在当前数据库复现原始冻结指标。
- 后续所有研究、诊断和功能实验，都必须以B0.4（0bp，不计滑点）为A/B测试的对照组。
- 未经过独立验证并正式更新本文件的研究结果，不得取代B0.4成为新基线。

代码分支名称 `v1.2.1-regime-adaptive` 是历史开发分支名称，v1.2.3 继续使用此分支。32只ETF增强池是研究框架，不是生产主线。

---

## 2. B0.4 基准包含什么

### 2.1 ETF池：固定18只

| 类型 | 数量 | 用途 |
|------|------|------|
| 行业ETF | 16只 | 评分、排序和轮动选择 |
| 防御ETF | 2只 | 行业信号不足时提供黄金和国债防御敞口 |
| 合计 | **18只** | 当前基准可交易池 |

16只行业ETF：

- 半导体、软件、通信、医药、消费、新能源、汽车、银行
- 券商、军工、传媒、有色金属、家电、养殖、油气、机器人

2只防御ETF：

- `518880.SH` 黄金ETF
- `511010.SH` 国债ETF

沪深300指数用于基准比较和市场观察，不属于18只可交易ETF。

### 2.2 当前核心配置（B0.4 口径）

| 项目 | B0.4 口径 |
|------|----------|
| 核心池 | 16只行业ETF |
| 防御池 | 黄金ETF、国债ETF |
| 调仓频率 | 每周四 |
| 最大持仓数 | 5只 |
| 单只仓位上限 | 20% |
| 动量排名因子 | 关闭 |
| 波动率因子 | 关闭 |
| 生效评分 | 趋势强度、趋势确认、成交量 |
| 止损 | 固定止损-8% |
| 调仓引擎 | v2.5 纯函数调仓规划逻辑 |
| 市场状态 | 仅观察，不改变交易参数 |
| 宽基补仓 | 关闭 |
| 滑点 | 当前基线不计（0bp） |
| 数据准入 | v1.1（exit_code=1，WARN） |

---

## 3. 32只ETF池不是当前基准

代码中存在由16只行业ETF和16只概念ETF组成的32只增强池框架，这是v1.2.1阶段搭建的研究与扩展能力。

必须明确：

- 32只ETF池是**研究/增强框架**，不是当前生产基线。
- 概念ETF数据完整性、历史可交易性和动态准入仍需进一步验证。
- `fixed_32` 回测使用了不同的标的池和信息集合，不能代表B0/B0.4主线表现。
- `fixed_32` 的收益、回撤、交易次数或归因结果，不得写成“当前模型表现”。
- 只有明确加载 `ETF_UNIVERSE + DEFENSE_UNIVERSE` 的18只ETF回测，才属于B0/B0.4口径。

如未来要采用32只增强池，必须将其作为独立实验组，与冻结B0.4进行完整A/B验证，而不能直接覆盖基线。

---

## 4. 当前交易执行口径

当前主线采用：

> **T日收盘后生成信号，T+1交易日开盘执行交易。**

实现口径：

- 趋势、均线、成交量等信号只使用截至T日收盘时已经可获得的数据。
- 回测在下一交易日使用开盘价执行买卖。
- 该口径避免使用尚未完成的当日收盘数据在同一收盘价成交。

注意：`src/config.py` 中 `EXECUTION_CONFIG['price_mode']='close'` 属于尚未接入当前回测执行路径的旧配置说明；实际基准成交口径以 `src/backtest.py` 使用当日 `open` 执行的实现为准。后续应单独清理这处文档性歧义，但不得在清理时改变B0.4交易结果。

---

## 5. 滑点口径与稳健性

**B0.4 基线暂不计滑点（0bp）。**

已完成单变量滑点敏感性测试（0/3/5/10 bp），结果如下：

| 滑点 | 最终NAV | 年化 | 夏普 | 最大回撤 |
|------|---------|------|------|----------|
| 0 bp | 2,761,288 | 16.68% | 0.8816 | -17.75% |
| 3 bp | 2,567,821 | 15.40% | 0.8162 | -18.55% |
| 5 bp | 2,488,278 | 14.85% | 0.7870 | -19.08% |
| 10 bp | 2,301,964 | 13.50% | 0.7154 | -20.40% |

- 夏普单调递减，无伪改善。
- 滑点直接成本占NAV差异约34%~45%，其余来自持仓路径变化。
- **滑点测试不改变0bp冻结基线**，仅用于稳健性评估。

进入更大资金规模或实盘准备阶段后，应以滑点测试结果为参考，但不得根据测试结果反向调优其他参数。

---

## 6. 当前主线表现以什么为准

当前主线评估只能使用B0/B0.4的长期回测和封存OOS结果。

### 6.1 B0.4 长期基准

权威报告：`reports/baseline_B0.3_20260620_180745.md`（B0.4 使用相同配置，数据更新）

| 指标 | B0.4 |
|------|------|
| 最终NAV | 2,761,288.07 |
| 总收益 | 176.13% |
| 年化收益 | 16.68% |
| 夏普比率 | 0.8816 |
| 最大回撤 | -17.75% |
| 交易次数 | 804 |
| 买入次数 | 399 |
| 卖出次数 | 405 |
| 止损次数 | 17 |
| 总佣金 | 68,826元 |

上述结果对应冻结的18只ETF B0.4配置，不得与 `fixed_32` 或其他增强池结果混用。

### 6.2 封存OOS验证

权威报告：`reports/phase5_final_oos.md`

OOS区间为2025-01-01至2026-06-18。B0.4所采纳的 `no_momentum` 配置结果：

| 指标 | OOS结果 |
|------|---------|
| 年化收益 | 42.16% |
| 夏普比率 | 1.9086 |
| 最大回撤 | -11.36% |

该OOS区间已经封存，不得继续用于参数选择或反向调优。

---

## 7. Phase 5/6/7/8 的正确定位

| 阶段 | 定位 | 是否生产基线 |
|------|------|--------------|
| Phase 5 | 因子、参数和OOS验证 | 否；验证结果可用于决定是否更新基线 |
| Phase 6 | 市场结构、止损、节假日、持仓与稳健性诊断 | 否 |
| Phase 7 | ETF池历史完整性、幸存者偏差等研究 | 否 |
| Phase 8 | 排名-仓位诊断、滑点压力测试 | 否 |

这些阶段可能：

- 以B0.4为对照开展研究；
- 测试增强模块；
- 发现数据、回测或策略风险；
- 提出候选改进。

但在完成单变量A/B测试、样本内外验证、风险检查和正式基线更新之前，它们都不是生产基线。

---

## 8. 版本、基线、引擎独立命名

| 维度 | 当前版本 |
|------|----------|
| 策略版本 | v1.2.3 |
| 基线版本 | B0.4 |
| 调仓引擎 | v2.5 |
| 数据准入 | v1.1 |
| 滑点测试 | v2 |
| 虚拟实盘 | v0.3 |

六者独立演进：
- 策略版本（v1.x）描述整体策略能力演进；
- 基线版本（B0.x）描述冻结对照状态；
- 引擎版本（v2.x）描述调仓规划器实现；
- 数据准入版本（v1.x）描述数据质量检查流程；
- 滑点测试版本（v2）描述稳健性验证口径；
- 虚拟实盘版本（v0.x）描述 Streamlit 虚拟盘与纸面交易功能演进。

---

## 9. Paper Trading v0.3 虚拟盘

Paper Trading 采用独立版本号（`DECISIONS.md` D-014），当前为 **v0.3**，不提升策略版本号。

### 9.1 页面入口

启动 Streamlit 后，顶部第 7 个页签为“虚拟盘”：

```bash
streamlit run app.py
```

### 9.2 五个子页面

1. **账户总览**：年化收益、夏普、最大回撤、Calmar、胜率、换手、佣金、交易次数。
2. **创建账户**：
   - 对比账户：从策略预设批量创建，同批次共享 `group_id`、初始资金和起始日期。
   - 影子账户：手动录入名称、总资产、现金、持仓明细和起始日期。
3. **今日运行**：选择账户后运行当日策略，生成 NAV、订单和成交记录。
4. **待确认订单**：展示影子账户的 PENDING 订单，支持确认成交、标记未执行/拒绝、取消。
5. **账户详情与策略对比**：查看历史订单、净值曲线、回撤曲线、持仓明细，并与同批次 B0.4 对比账户对比。

### 9.3 对比账户 vs 影子账户

- **对比账户**：自动执行，运行后生成 FILLED 成交和 NAV 记录，用于与 B0.4 做并行虚拟盘跟踪。
- **影子账户**：运行后仅生成 PENDING 影子订单，用户逐笔确认后才更新现金、持仓和 NAV，用于跟踪用户自己的实盘或模拟账户。

### 9.4 资金勾稽（来自真实测试）

- 现金 + 持仓市值 = 总资产
- 佣金已计入现金变化
- 买入不得造成负现金
- 卖出不得超过实际持仓

### 9.5 验证与已知限制

- 182 项自动化测试通过 + 1 项 Playwright 浏览器验收测试通过。
- 不修改 B0.4 策略、参数、ETF 池或冻结数据。
- 当前限制：无结束账户、无永久删除账户、无定时自动运行、不连接券商。

---

## 10. 后续演进路线

后续固定按以下路线推进：

1. **B0.4 18只ETF冻结基线**
2. **稳健性验证**（滑点已完成，下一步待定）
3. **Paper Trading v0.3.1**：账户生命周期扩展（结束账户、删除账户）。
4. **小步接入市场状态、持仓稳定或仓位优化**（以 B0.4 为对照）。
5. **每次只修改一个变量并进行A/B测试**。
6. **只有明确优于B0.4且风险未恶化的方案，才有资格成为候选**。
7. **候选经正式验收后，才更新冻结基线和本文件**。

当前下一步：完成版本说明补全，随后进入 Paper Trading v0.3.1 账户生命周期开发。

---

## 11. 项目统一锚点

后续讨论、报告和代码审查统一使用以下表述：

> **当前策略版本：v1.2.3；当前主线：B0 18只ETF基准版；当前冻结对照：B0.4；虚拟实盘：v0.3。**
>
> **演进方法：稳健性验证 → 小步接入增强模块 → 每次只做一个变量的A/B测试。**

如其他文档、报告或聊天记录与本口径冲突，以本文件、B0.4基准脚本及新鲜回测验证结果为准。

---

*变更历史参见 `docs/CHANGES.md`。本文档只定义当前主线，不承载完整研究流水账。*
