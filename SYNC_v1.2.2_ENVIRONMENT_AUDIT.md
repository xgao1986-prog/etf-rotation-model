# Sync Document - v1.2.2 测试环境审计

**生成时间**: 2026-06-16
**负责人**: Kimi
**目标读者**: Codex + 用户
**主题**: 104.76%基线 vs 当前回测环境差异分析

---

## 1. 用户意图声明

> "我不是要把数字做好看，而是要把策略测试环境设置得尽量接近实盘。然后后续再优化策略表现。"

这意味着：
- **修复环境问题**（回测环境与实盘不一致的bug）
- **记录策略变化**（但不急于优化策略参数）
- **建立可重复、可审计的测试环境**

---

## 2. 104.76%基线 vs 当前环境：差异全景

### 2.1 历史基线（2026-06-13）

| 项目 | 配置 |
|------|------|
| 总收益 | **104.76%** |
| 夏普 | **0.78** |
| 最大回撤 | **-14.41%** |
| 交易次数 | 678 |
| ETF池 | 16只原始池（ETF_UNIVERSE） |
| 宽基补仓 | **关闭**（fallback_equity_enabled=False） |
| 防御填充 | **开启**（defense_enabled=True） |
| 市场择时 | **关闭**（market_timing=False） |
| 交易执行价 | **open**（开盘执行） |
| 调仓日 | 周五（rebalance_weekday=4） |
| 数据来源 | 16只均为iFinD forward复权 |

### 2.2 当前环境（2026-06-16）

| 项目 | 配置 |
|------|------|
| 总收益 | 48.96%（32只池） / 6.65%（16只池） |
| 夏普 | 0.35（32只） / 0.05（16只） |
| 最大回撤 | -21.41%（32只） / -40.41%（16只） |
| 交易次数 | 837（32只） / 580（16只） |
| ETF池 | 32只（CORE_UNIVERSE） |
| 宽基补仓 | **配置关闭，但代码不检查配置**（bug） |
| 防御填充 | 开启（但v1.2.2空仓机制减少了触发） |
| 市场择时 | 关闭 |
| 交易执行价 | open（未变） |
| 调仓日 | 周五（未变） |
| 数据来源 | 混合（22只forward + 20只qfq） |
| 数据库修改 | **2026-06-15 09:33**（基线后1-2天） |

---

## 3. 环境差异分析（按影响排序）

### 3.1 🔴 宽基补仓bug（已修复）

**问题**: `backtest.py` 在备选池兜底逻辑中**没有检查 `fallback_equity_enabled` 配置**。

**代码位置**: `src/backtest.py:933`
```python
# 修复前（bug）：
if max_new > 0 and available_cash > 1000:
    # 直接执行宽基补仓，不检查配置

# 修复后：
if (max_new > 0 and available_cash > 1000 and 
        self.cfg.get('fallback_equity_enabled', False)):
    # 只在配置开启时执行
```

**影响**: 即使配置中 `fallback_equity_enabled=False`，回测仍然会买入宽基ETF（510300.SH/510500.SH/159915.SZ/588000.SH）。实盘不会买入这些ETF，导致回测与实盘不一致。

**修复效果**:
- 32只池：48.96% → 55.88%（+6.9pp）
- 宽基买入次数：22次 → 0次

**状态**: ✅ 已修复（commit db46245 + 1）

### 3.2 🔴 数据库修改（2026-06-15）

**问题**: 数据库文件 `etf_model.db` 在 **2026-06-15 09:33** 被修改，而104.76%基线验证于 **2026-06-13**。

**数据变化**:
- 数据库从约40,000条增加到57,312条
- 新增/补齐了多只ETF数据（159566.SZ、560700.SH、588200.SH等）
- 部分ETF数据从forward切换为qfq（如512690.SH）
- 部分ETF历史数据被扩展（如510880.SH从2022年扩展到2019年）

**验证**:
- 回退到104.76%基线代码（commit 4694e9f），当前数据上只能跑出 **26.25%**
- 回退到v1.1代码（commit 1539013），当前数据上只能跑出 **6.65%**
- 证明：代码不是主因，**数据变化是主因**

**状态**: ⚠️ 数据更新是正常维护，但需要记录数据版本与回测结果的对应关系

**建议**: 在数据库中增加 `run_logs` 表记录数据更新时间，或在回测报告中标注数据版本。

### 3.3 🟡 v1.2.2空仓机制（策略优化，非环境问题）

**问题**: 用户指出"当所有板块都差时，模型不应选择股票型ETF"。修复后引入了"全场质量检测"。

**代码位置**:
- `src/strategy.py:rank_all_momentum()` - 差市场时momentum_rank=0
- `src/strategy.py:generate_signals()` - 差市场时min_total_score从40提高到55

**影响**:
- 减少了"矬子里拔将军"的亏损信号
- 2021-2026间减少了约26.3%的无效BUY信号
- 这是**策略优化**，不是环境问题

**状态**: ✅ 策略优化，已验证有效（修复前-7.94% → 修复后+27.49%）

### 3.4 🟡 ETF池从16只扩展到32只（策略变化）

**问题**: 16只原始池 → 32只（+16只概念ETF）。

**影响**:
- 更多标的 = 更多轮动机会，但也 = 更多错误选择机会
- 32只池中部分ETF上市时间较晚（如159530.SZ 2024年才上市），历史数据不足
- 动态池评估在2019年起步时只有5只Core，27只在Watch池

**状态**: ⚠️ 策略变化，需要进一步验证32只池是否优于16只池

### 3.5 🟢 数据复权方式混合（当前无问题，但需监控）

**问题**: 数据库中22只ETF用iFinD `forward` 复权，20只用AKShare `qfq` 复权。

**当前状态**: 每个ticker只有一种adjust_type，没有重复数据，回测不会取到冲突价格。

**风险**: 如果未来同一只ETF同时导入两种复权数据，`get_market_data()` 没有按adjust_type过滤，会导致数据重复。

**状态**: 🟢 当前无问题，但建议增强 `get_market_data()` 添加adjust_type过滤参数

### 3.6 🟢 交易执行价（open vs close）

**当前状态**: 代码使用 `day_prices = market_df[market_df['date'] == date].set_index('ticker')['open'].to_dict()`，即**开盘执行**。

104.76%基线也是开盘执行（从PROGRESS.md记录确认）。

**状态**: ✅ 一致，无需修改

### 3.7 🟢 手续费配置

**当前状态**: `commission_rate=0.0003`（0.03%），`min_commission=5元`。

与基线一致。

**状态**: ✅ 一致，无需修改

---

## 4. 修复清单

### 4.1 已修复

| 问题 | 修复位置 | 提交 |
|------|---------|------|
| 宽基补仓不检查配置 | `src/backtest.py:933` | ca6749a + 1 |
| data_days负数 | `src/universe_builder.py` | d98bd03 |
| universe_builder返回值解析 | `src/backtest.py` | d98bd03 |

### 4.2 待修复（建议）

| 问题 | 优先级 | 说明 |
|------|-------|------|
| 数据库版本追踪 | 高 | 增加数据版本字段，记录导入时间 |
| get_market_data添加adjust_type过滤 | 中 | 防止未来数据重复 |
| 回测报告标注数据版本 | 中 | 每次回测记录数据MD5或时间戳 |
| 动态池滚动评估 | 中 | 从固定起点改为每年重新评估 |
| 防御填充触发条件验证 | 低 | 确认v1.2.2是否减少了防御填充触发 |

---

## 5. 测试环境建议

### 5.1 标准测试配置（建议采用）

```python
# 标准测试环境（接近实盘）
标准配置 = {
    'ETF池': '32只CORE_UNIVERSE',  # 或16只原始池，需要明确选择
    '宽基补仓': False,  # 当前参数下负贡献，实盘不启用
    '防御填充': True,  # 股票ETF不足时填充防御资产
    '市场择时': False,  # 回测显示关闭后收益更高
    '交易执行价': 'open',  # 消除look-ahead bias
    '调仓日': 'Friday',  # 每周五调仓
    '手续费': 0.03% + 最低5元,
    '数据来源': 'iFinD forward + AKShare qfq（混合）',
    '初始资金': 100万,
}
```

### 5.2 环境一致性检查清单

每次回测前，确认：
- [ ] 数据库未在回测前24小时内被修改（或已记录修改内容）
- [ ] 所有ETF数据覆盖回测区间（无缺失）
- [ ] 每只ETF只有一种adjust_type
- [ ] 宽基补仓配置与代码行为一致
- [ ] 回测脚本使用正确的ETF池（16或32只）
- [ ] 回测结果文件标注数据版本和代码版本

---

## 6. 数据版本问题详细说明

### 6.1 为什么数据库被修改？

从git历史看，6月14日进行了多次数据补齐操作：
- 999fabe: 补齐513160.SH数据，清理多余ETF
- 0f5cda6: 增加adjust_type字段，清理512010.SH/512480.SH的AKShare数据
- 446a8e5: 扩展32只池，导入多只ETF历史数据

这些修改的目的是**统一数据口径**（从混用AKShare/iFinD到标记adjust_type），但可能同时改变了部分ETF的价格序列。

### 6.2 数据变化对回测的影响

**正面影响**:
- 更多ETF有完整历史数据（如510880.SH从2022扩展到2019）
- 数据口径统一（标记adjust_type）
- 清理了重复/多余ETF数据

**负面影响**:
- 部分ETF数据源切换（forward→qfq或反之），价格序列可能不同
- 新导入的历史数据可能存在数据质量问题
- 与104.76%基线的数据状态不一致，导致无法直接对比

### 6.3 建议的数据管理流程

1. **数据导入前备份**: 每次批量导入前，备份 `etf_model.db`
2. **数据验证**: 导入后运行数据质量检查（缺失值、异常价格、复权一致性）
3. **基线重跑**: 数据更新后，重跑标准基线回测，确认环境一致
4. **版本标记**: 在数据库中记录数据版本号，回测报告标注版本

---

## 7. 代码变更审计

### 7.1 v1.2.1 → v1.2.2 变更

**策略变更**（用户驱动，非环境问题）:
- `src/strategy.py:rank_all_momentum()` - 差市场时momentum_rank=0
- `src/strategy.py:generate_signals()` - 差市场时min_total_score提高到55

**环境修复**（回测环境一致性问题）:
- `src/backtest.py:933` - 宽基补仓检查fallback_equity_enabled配置
- `src/universe_builder.py` - 修复data_days负数
- `src/backtest.py` - 修复universe_builder返回值解析

**新增功能**:
- `run_walk_forward_backtest.py` - Walk-forward回测脚本
- `src/universe_builder.py` - 动态池评估器

### 7.2 建议的代码审查点

1. **所有配置开关是否被代码检查**: 搜索 `cfg.get('xxx_enabled')` 是否在每个相关逻辑处都被检查
2. **数据源一致性**: 确认同一只ETF不会同时存在两种adjust_type
3. **回测脚本默认配置**: 确认 `run_walk_forward_backtest.py` 和 `main.py` 使用相同的默认配置

---

## 8. 下一步建议（用户优先）

### 8.1 立即行动（环境修复）

1. **数据版本标记**: 在数据库中增加 `data_version` 表，记录每次导入的时间、内容和影响范围
2. **基线重跑**: 在当前数据上，使用标准配置（16只池+宽基关闭+防御开启+择时关闭）重跑基线，建立新的基准
3. **配置一致性检查**: 添加单元测试，验证所有 `xxx_enabled` 配置都被代码正确检查

### 8.2 后续优化（策略改进）

1. **32只池 vs 16只池对比**: 在统一数据上，跑16只和32只的对照回测，确认扩展是否有价值
2. **动态池滚动评估**: 将固定起点评估改为每年重新评估，避免错过新上市ETF的牛市表现
3. **空仓机制参数调优**: 测试不同的min_total_score差市场门槛（50/55/60），找到最优值

### 8.3 不要做的

- ❌ 不要试图复现104.76%（数据已变，无法复现）
- ❌ 不要盲目优化参数（先确认环境一致）
- ❌ 不要继续扩展ETF池（先验证32只池的价值）

---

## 9. 关键结论

1. **104.76%基线无法在当前数据上复现** — 主因是数据库在6月15日被修改，数据已变化
2. **宽基补仓bug是真实的环境问题** — 已修复，修复后32只池从48.96%提升到55.88%
3. **v1.2.2空仓机制是策略优化** — 不是环境问题，已验证有效（修复前-7.94% → 修复后+27.49%）
4. **当前数据是"更好"的数据** — 更完整、口径统一，但需要建立新的基线
5. **建议先建立标准测试环境** — 明确ETF池（16或32只）、配置开关、数据版本，然后重跑基线

---

## 10. 文件位置

- 本Sync文档: `SYNC_v1.2.2.md`
- 回测报告: `reports/walk_forward_pool_backtest.md`
- 回测数据: `reports/walk_forward_pool_backtest.csv`
- 代码修复: `src/backtest.py`（宽基补仓配置检查）
- 策略修复: `src/strategy.py`（空仓机制）
- 数据库: `database/etf_model.db`（2026-06-15修改）

---

*本文档由Kimi生成，记录v1.2.2测试环境审计结果，供Codex审查和继续开发使用*
