# ETF轮动模型协作规则

## 新线程启动

新对话或上下文恢复时，按顺序读取：

1. `AGENTS.md`
2. `docs/CURRENT_STATE.md`
3. `docs/DECISIONS.md`
4. `git status --short --branch`

不要默认完整读取 `KIMI_CODEX_SYNC.md`、`HANDOFF.md`、全部报告或旧对话。只有当前问题确实需要历史证据时，才读取对应章节或文件片段。

若上述文件与实际代码、测试或Git状态冲突，以新鲜的本地验证结果为准，并更新 `docs/CURRENT_STATE.md`。

## 工作区保护

- 工作区长期存在大量未提交研究文件和修改；不得清理、覆盖、重置或移动无关改动。
- 禁止使用 `git reset --hard`、`git checkout --` 或等价破坏性操作，除非用户明确授权。
- 修改前先检查目标文件状态；只处理当前任务范围内的文件。
- 不因报告、聊天或Kimi声称“完成”而视为完成，必须读取实际文件并运行新鲜验证。

## 研究与策略纪律

- 一次只研究或修改一个变量。
- 冻结基线不得被实验覆盖；实验使用独立开关、脚本或文件。
- 不根据样本外结果反向挑参数。
- 区分工程修复、研究实验和正式策略变更。
- 未完成数据口径、NAV、交易记录或样本数勾稽前，不下策略结论。
- 报告中的假设必须标为假设，不能写成已证明结论。

## 代码与验收

- 功能或缺陷修复先建立能够复现问题的测试。
- 完成声明前运行与风险相称的测试、编译或回测，并报告真实输出。
- 通过现有测试不等于满足需求；审查测试是否削弱、遗漏边界或只验证汇总值。
- 财务计算至少检查：现金、持仓市值、佣金、总NAV、整手、成交价格口径和订单顺序。
- 代码审查优先报告可复现问题，标注文件和行号。

## 文档职责

- `docs/CURRENT_STATE.md`：唯一当前现场；阶段变化或交接前覆盖更新，目标不超过300行。
- `docs/DECISIONS.md`：长期有效的确认决策；只追加、修订或明确废止。
- `KIMI_CODEX_SYNC.md`：历史协作流水账，不作为启动入口。
- `HANDOFF.md`：旧交接材料，仅作历史参考。
- `reports/`：研究证据和详细结果，不复制整篇到当前状态。

## 上下文控制

- 大型输出写入文件，对话中只给结论、关键数值和路径。
- 优先使用定向搜索与分段读取，避免输出完整大文件、完整数据库结果或巨大diff。
- 一个线程只承担一个清晰阶段；阶段验收后更新当前状态并开启新线程。
- 对话接近上限时，先更新 `docs/CURRENT_STATE.md`，再停止扩展新任务。

## 标准交接提示

新线程可使用：

> 请先读取 `AGENTS.md`、`docs/CURRENT_STATE.md`、`docs/DECISIONS.md` 和当前 `git status`，恢复现场。不要完整读取历史日志，除非当前任务需要。先复核当前断点，再继续唯一下一步。

